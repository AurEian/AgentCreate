# 2026-03-30 工作日志

## Glass Blog 前端重写完成

### 完成内容
- 将前端从单一大文件拆分为模块化文件：
  - `style.css`：完整 CSS（毛玻璃风格、亮/暗主题切换、响应式）
  - `app.js`：所有 API 函数 + SPA hash 路由 + 初始化逻辑
  - `pages.js`：登录/注册/首页/文章详情/评论渲染
  - `pages2.js`：Markdown 写作编辑器（实时预览+草稿自动保存）/草稿箱/收藏/关注
  - `pages3.js`：个人主页/管理后台（统计/文章/用户/评论/日志管理）
  - `index.html`：骨架页面，引用所有文件

### 修复的 Bug
1. seed 数据 comments 表字段数量不匹配
2. 后端 login 未返回 token（已补充 `token: user.id`）
3. 前端 Authorization header 格式不正确（去掉 Bearer 前缀）
4. getPosts 返回数据格式 normalize（tags 字符串→数组）
5. 新增后端端点：`/api/user/following`, `/api/user/:id/posts`

### 服务状态
- 已启动，运行在 http://localhost:3000
- 数据库已初始化，包含 5 篇测试文章和 3 个测试账号

## 下午修复汇总（关注系统 + 审核机制 + 项目结构整理）

### 修复的 Bug（下午）
- 关注系统：profile 页面 isFollowing 状态不同步 → 使用后端返回值 + 清除缓存
- 取关后列表未更新 → 添加滑出动画移除卡片
- 评论区头像/用户名 → 添加个人主页链接
- 已发布文章修改后旧版本继续展示 → pending_* 字段机制
- 审核通过/拒绝后页面不刷新 → 改为 setTimeout 后跳转当前路由重新渲染

### 项目结构整理
- 前端文件从根目录移入 `public/` 标准目录结构：
  - `public/index.html`、`public/css/style.css`、`public/js/*.js`、`public/uploads/`
- server.js 静态目录和 SPA fallback 指向 `public/`
- 清理根目录废弃文件（login.html、test_*.js/ps1、debug.ps1、server_log.txt）

### 后端模块化重构
- 将 1127 行的 server.js 拆分为模块化架构：
  - `src/db.js` — 数据库初始化 + seed + 查询辅助函数 + 通用工具
  - `src/middleware/auth.js` — 认证中间件（requireAuth/optionalAuth/requireAdmin）
  - `src/routes/` — 8 个路由模块（auth/user/posts/comments/notifications/announcements/admin/upload）
- `server.js` 精简为 ~40 行入口文件（配置 + 路由挂载 + 启动）
- 所有路由模块通过 `function setupXxxRoutes(app)` 模式导出
- login.html 原本已是废弃的（SPA 下登录在 pages.js 中动态渲染），直接删除
